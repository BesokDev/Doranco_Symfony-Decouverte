{% extends 'base.html.twig' %}

{% block body %}

    <div class="row mt-3 mb-5">
        <div class="col-10 mx-auto">

            <h1 class="text-center my-3">Plateforme de mise en relation</h1>
            <hr>
            {% include 'include/_flashes.html.twig' %}
        </div>
    </div>


    <div class="row">
        <div class="col-6">

            <h3 class="text-center my-3">Tous les candidats</h3>

            <table class="table">
                <thead>
                    <tr>
                        <th>Prenom</th>
                        <th>Nom</th>
                        <th>Email</th>
                        <th>Age</th>
                        <th>Age</th>
{#                        <th>Actions</th>#}
                    </tr>
                </thead>
                <tbody>

                    {% for candidat in candidats %}
                        <tr>
                            <td>{{ candidat.prenom }}</td>
                            <td>{{ candidat.nom }}</td>
                            <td>{{ candidat.email }}</td>
                            <td>{{ candidat.age  }}</td>
                            <td>{{ candidat.candidatedAt | date('d/m/Y') }} €</td>
{#                            <td>#}
{#                                <a href="{{ path('update_candidat', {'id': candidat.id}) }}" class="btn btn-info" title="Modifier un employé"><i class="fa fa-edit"></i></a> |#}
{#                                <a href="{{ path('delete_candidat', {'id': candidat.id}) }}" class="btn btn-danger" title="Supprimer un employé"><i class="fa fa-trash"></i></a>#}
{#                            </td>#}
                        </tr>
                    {% endfor %}

                </tbody>
            </table>

        </div>
{# ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////// #}
        <div class="col-6">

            <h3 class="text-center my-3">Toutes les entreprises</h3>

            <table class="table">
                <thead>
                <tr>
                    <th>ai</th>
                    <th>fd</th>
                    <th>dfdf</th>
                    <th>dfdf</th>
                    <th>dfdf</th>
                </tr>
                </thead>
                <tbody>
                <tr>
                    <td>fhjdfd</td>
                    <td>fgf</td>
                    <td>dfdgd</td>
                    <td>fgf</td>
                    <td>rer</td>
                </tr>
                </tbody>
            </table>
        </div>
    </div>

{% endblock %}